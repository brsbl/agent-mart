name: CI

on:
  pull_request:
    branches: [main]
    paths:
      - 'web/public/data/**'
      - 'data/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Skipping tests for data-only PR"
